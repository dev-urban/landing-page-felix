---

---


<section id="contato" class="section contact">
  <div class="container">
    <div class="section-header">
      <h2>Garanta seu studio agora</h2>
      <p>Não perca esta oportunidade única de investimento em um studio premium. Fale com nossa equipe especializada.</p>
    </div>

    <div class="contact-content">
      <div class="contact-form-container">
        <form class="contact-form">
          <div class="form-row">
            <div class="form-group">
              <label for="firstName">Nome completo *</label>
              <input type="text" id="firstName" name="firstName" placeholder="Digite seu nome completo" required>
            </div>
            <div class="form-group">
              <label for="email">E-mail *</label>
              <input type="email" id="email" name="email" placeholder="seu@email.com" required>
            </div>
          </div>

          <div class="form-group">
            <label for="phone">Telefone com DDD *</label>
            <input type="tel" id="phone" name="phone" placeholder="(51) 99999-9999" required>
          </div>

          <div class="form-group">
            <label for="message">Mensagem (opcional)</label>
            <textarea id="message" name="message" rows="4" placeholder="Conte-nos mais sobre seu interesse no Square Garden"></textarea>
          </div>

          <button type="submit" class="btn btn-primary">Quero meu studio no Square Garden</button>
        </form>
      </div>

      <div class="contact-info">
        <div class="contact-item">
          <div class="contact-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
              <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              <circle cx="12" cy="10" r="3" stroke="currentColor" stroke-width="2"/>
            </svg>
          </div>
          <div class="contact-details">
            <h4>Endereço</h4>
            <p>Av. Tulio de Rose, 200<br>Porto Alegre/RS</p>
          </div>
        </div>

        <div class="contact-item">
          <div class="contact-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
              <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div class="contact-details">
            <h4>Telefone</h4>
            <p>(51) 3407-0271</p>
          </div>
        </div>

        <div class="contact-item">
          <div class="contact-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
              <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              <polyline points="22,6 12,13 2,6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div class="contact-details">
            <h4>E-mail</h4>
            <p>contato@urban.imb.br</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes slideInLeft {
    from {
      opacity: 0;
      transform: translateX(-40px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes slideInRight {
    from {
      opacity: 0;
      transform: translateX(40px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  .contact {
    background: #111909;
    color: white;
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-header h2 {
    color: white;
    margin-bottom: 1rem;
    background: linear-gradient(135deg, #22c55e, #16a34a);
    display: inline-block;
    padding: 0.5rem 1rem;
    border-radius: 7px;
  }

  .section-header p {
    color: #bcbec0;
    font-size: 1.125rem;
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: 4rem;
    align-items: start;
  }

  .contact-form-container {
    background: white;
    border: 1px solid #111909;
    border-radius: 45px;
    padding: 3rem;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .contact-form-container:hover {
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .form-group label {
    font-weight: 500;
    color: #111909;
  }

  .form-group input,
  .form-group textarea {
    padding: 1rem;
    border: 1px solid #d9d9d9;
    border-radius: 14px;
    font-family: inherit;
    font-size: 1rem;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .form-group input:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: #5b6f42;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(91, 111, 66, 0.15);
  }

  .form-group textarea {
    resize: vertical;
    min-height: 120px;
  }

  .contact-info {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .contact-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 1.5rem;
    background: #5b6f42;
    border-radius: 20px;
    border: 1px solid #5b6f42;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .contact-item:hover {
    transform: translateX(10px);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
  }

  .contact-icon {
    flex-shrink: 0;
    width: 50px;
    height: 50px;
    background: #111909;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
  }

  .contact-details h4 {
    font-size: 1.125rem;
    color: white;
    margin-bottom: 0.5rem;
  }

  .contact-details p {
    color: white;
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .contact-content {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .contact-form-container {
      padding: 2rem;
    }

    .form-row {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .contact-item {
      padding: 1rem;
    }

    .contact-icon {
      width: 40px;
      height: 40px;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {

    // Form submission
    const form = document.querySelector('.contact-form');

    form.addEventListener('submit', function(e) {
      e.preventDefault();

      // Get form data
      const formData = new FormData(form);
      const firstName = formData.get('firstName');
      const email = formData.get('email');
      const phone = formData.get('phone');

      // Simple validation
      if (!firstName || !email || !phone) {
        alert('Por favor, preencha todos os campos obrigatórios.');
        return;
      }

      // Get optional message
      const message = formData.get('message') || '';

      // Submit to webhook
      const submitButton = form.querySelector('button[type="submit"]');
      const originalText = submitButton.textContent;

      submitButton.textContent = 'Enviando...';
      submitButton.disabled = true;

      // Prepare data for webhook
      const data = {
        body: {
          nome: firstName,
          email: email,
          telefone: phone
        }
      };

      // Send to webhook
      fetch('https://n8n.urban.imb.br/webhook/landing-pages', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify(data)
      })
      .then(response => {
        if (!response.ok) {
          throw new Error('Erro na rede');
        }
        return response.json().catch(() => ({})); // Handle non-JSON responses
      })
      .then(data => {
        // Send event to dataLayer
        if (window.dataLayer) {
          window.dataLayer.push({
            'event': 'form_submit',
            'form_name': 'contact_form',
            'form_location': 'contact'
          });
        }

        // Send conversion event to Google Ads
        console.log('Aguardando gtag carregar...');
        if (window.gtag) {
          console.log('gtag disponível, enviando conversão...');
          window.gtag('event', 'conversion', {
            'send_to': 'AW-11105885842/_TeYCJiCj8EZEJK92a8p'
          });
          console.log('Conversão enviada com sucesso!');
        } else {
          console.warn('gtag ainda não carregou, tentando aguardar...');
          setTimeout(function() {
            if (window.gtag) {
              console.log('gtag carregou após espera, enviando conversão...');
              window.gtag('event', 'conversion', {
                'send_to': 'AW-11105885842/_TeYCJiCj8EZEJK92a8p'
              });
              console.log('Conversão enviada com sucesso!');
            } else {
              console.error('gtag não carregou mesmo após espera');
            }
          }, 500);
        }

        alert('Obrigado pelo seu interesse no Square Garden! Nossa equipe entrará em contato em breve.');
        form.reset();
      })
      .catch(error => {
        console.error('Erro:', error);
        alert('Obrigado pelo seu interesse no Square Garden! Nossa equipe entrará em contato em breve.');
        form.reset();
      })
      .finally(() => {
        submitButton.textContent = originalText;
        submitButton.disabled = false;
      });
    });
  });
</script>